name: Configure VM with Ansible

on:
  push:
    branches:
      - main
    paths:
      - "ansible-playbooks/**"         # Triggers if files in the ansible directory are changed
      - "azure-terraform/**"  # Triggers if Terraform files are modified (optional)
  workflow_dispatch:          # Allows for manual triggering

jobs:
  configure-vm:
    name: Configure VM
    runs-on: ubuntu-latest
    steps:
      - name: Check out the code
        uses: actions/checkout@v2

      - name: Download VM Public IP
        uses: actions/download-artifact@v4  # Updated to v4
        with:
          name: vm_public_ip

      - name: Load VM Public IP
        run: |
          export VM_PUBLIC_IP=$(cat vm_public_ip.txt)

      - name: Set up SSH Key
        run: |
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ssh_key.pem
          chmod 600 ssh_key.pem

      - name: Copy Ansible Playbook to VM
        run: |
          scp -o "StrictHostKeyChecking=no" -i ssh_key.pem ansible/playbook.yml azureuser@$VM_PUBLIC_IP:/home/azureuser/

      - name: SSH into VM to Install Ansible and Run Playbook
        run: |
          ssh -o "StrictHostKeyChecking=no" -i ssh_key.pem azureuser@$VM_PUBLIC_IP << EOF
            sudo apt update
            sudo apt install -y ansible
            ansible-playbook -i "localhost," -c local /home/azureuser/playbook.yml
          EOF
